import React, { FunctionComponent } from "react";

type Props = {
    numbers: Array<number>;
};

const InsertionSort: FunctionComponent<Props> = ({
    numbers = [9, 3, 2, 5, 8, 2],
}) => {
    return <div></div>;
};

export default InsertionSort;
